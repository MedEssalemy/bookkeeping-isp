{
  "name": "bookkeeping",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm --filter frontend build",
    "deploy": "pnpm --filter frontend build && pnpm --filter frontend exec wrangler deploy --cwd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.7.1"
}
